keycloak:
  http:
    httpEnabled: true
    tlsSecret: ""
  ingress:
    enabled: true
    className: nginx
    tlsSecret: ""
  hostname: ""

  # Embedded PostgreSQL StatefulSet (templates/postgres.yaml).
  postgres:
    user: ""
    password: ""
    persistence:
      enabled: true
      size: 5Gi
      # Omit or leave empty to use the cluster default StorageClass.
      storageClass: ""